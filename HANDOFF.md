# HANDOFF.md

## 1. 当前目标

当前任务目标分三段：

- 保持既有文档与交接文件可持续更新；
- 修正导出版式与主题 query pack，使结果更偏硬件、供应链、量产和中国落地；
- 继续拉宽核心时间线，避免时间线只剩会展开成长文的少数事件，尤其修复“长文已生成但时间线没有对应主档”的脱锚问题；
- 在不影响 `24h` 日报的情况下，将 `7天` 周报从当前共用链路中独立出来，使其能覆盖一周热点并输出主题总结。

## 2. 已完成内容

- 已确认当前 Git 同步仓库为 `E:\Users\zwz10\PycharmProjects\_collectNews_publish_verify`。
- 已确认本地带 key 的运行副本为 `E:\Users\zwz10\PycharmProjects\collectNewslocal`。
- 已确认同步仓库中此前不存在 `AGENTS.md`、`PLANS.md`、`HANDOFF.md`。
- 已定位当前任务的关键代码位置：
  - K 线生成：`tools/finance_engine.py`
  - 金融页图表插入：`tools/export_ppt.py`
  - 行业主题包：`tools/intelligence_packs.py`
  - 公司 query pack 与排序：`tools/company_query_packs.py`
- 已新增三份文档，完成规则、计划和交接的基础落盘。
- 已完成主题强化：
  - Apple 增加折叠机、铰链、UTG、模组、供应链、中国制造相关 query/keyword。
  - Google 增加 TPU、Ironwood/Trillium、Tensor、Pixel Fold、算力硬件相关 query/keyword。
  - Tesla 增加 FSD 中国落地、审批/试点、城市导航、上海工厂等 query/keyword。
  - Amazon、Meta 和 generic fallback pack 也同步向硬件/供应链方向收紧。
- 已完成行业扩展主题结构补全：
  - `tools/intelligence_packs.py` 中 6 个扩展主题已补齐 `tags` / `keywords` / `companies` / `domains` / `china_domains`，不再只是 query 文案。
- 已完成核心时间线策略调整：
  - 时间线事件目标数量从偏保守的 5-8 条放宽到 8-12 条，最多 14 条。
  - 时间线事件去重阈值放宽，减少不同事件被过早并掉的情况。
  - 明确要求“时间线不等于深度新闻候选池”，允许保留有意义但不一定进入长文的更新。
- 已完成时间线展示瘦身：
  - Streamlit 预览不再逐条显示长段“匹配原因”。
  - PPT 核心时间线页改为简短“后文承接”提示，不再塞整段匹配解释。
- 已完成 K 线图版式修复：
  - PPT 金融页图表宽度从 `8.2"` 缩到 `5.45"`，并右移到更安全的位置，避免压到下方催化剂文本框。
- 已完成第二轮核心时间线放宽：
  - 时间线目标数量进一步上调到“至少 7 条，常规 8-12 条，最多 16 条”。
  - 时间线输入窗口扩大：公司流蓝图候选从 `18` 提高到 `24`，行业流从 `16` 提高到 `20`。
  - 去重阈值继续放宽，降低不同短讯被误并为同一事件的概率。
  - `policy` / `generic` / `business` 配额提高，避免政策、地缘和市场短讯被过早压掉。
  - 标题回写阶段对低相似度原始结果的强行覆盖阈值提高，减少多条短讯被同一条新闻标题“吸过去”的问题。
- 已完成特朗普专题补强：
  - `tools/company_query_packs.py` 中 `trump` pack 已加入 Middle East / Israel / Iran / Gaza / Red Sea / oil / sanctions / defense / ceasefire 等关键词、优先词和 query。
  - 权威域名范围扩展到 `axios.com`、`politico.com`、`ft.com`、`aljazeera.com`，用于补足传统经贸线之外的地缘与外交线。
  - 特朗普专题的 focus hint 已改成政策 / 关税 / 中东 / 能源 / 制裁 / 外交导向，不再套用硬件公司模板。
- 已完成当前运行模式收敛：
  - 侧边栏已改为默认只走 DeepSeek。
  - Gemini 主模型/轻任务切换在本轮中已临时下线，不再作为当前排障和验证范围。
- 已完成时间线/长文对齐修复：
  - 已确认根因之一不在 `timeline_agent` 本身，而在 `tools/report_linker.py` 只做“时间线正向匹配长文”，没有处理“长文反向补线”。
  - 已新增反向补线逻辑：如果某条深度新闻在原始核心时间线中找不到可靠对应主档，系统会按长新闻标题、日期、来源自动补入一条时间线短讯。
  - 反向补入的时间线事件会与该长新闻共享同一个 `event_id`，并同步生成 `timeline_refs`，避免前后页面脱锚。
  - 已完成本地逻辑烟测：构造 “1 条时间线 + 3 条长新闻” 的案例后，输出可自动补成 “3 条时间线，且每条长新闻都有承接事件”。
- 已完成第三轮低 token 优化：
  - `agents/timeline_agent.py` 已加入基于现成 `raw_search_results` 的规则补位，不新增模型调用，也能把 AI 漏掉的短讯补回时间线。
  - 规则补位会综合标题清洗、结果支持度、来源分布、去重和同日排序，尽量补足短新闻数量，同时避免重复事件双开。
  - `agent_app.py` 的 `select_analysis_candidates()` 已改为优先追加“能支撑已有时间线事件”的结果，减少弱相关页面进入长文分析。
  - `tools/company_query_packs.py` 中 Tesla pack 已进一步补强中国 FSD 主线，加入备案、审批、试点城市、MIIT/监管和上海协同导向。
  - 已完成本地逻辑烟测：构造 “1 条时间线 + 4 条原始结果” 的 `Nvidia` 模拟案例后，最终时间线可稳定输出 3 条，且同一事件不再明显重复。
- 已完成第四轮“日报 / 周报分离”：
  - `agent_app.py` 已新增独立报告模式配置：`24h 日报`、`7天 周报`、`30天 观察` 分别对应独立时间窗口、搜索上限、蓝图输入上限、长文候选上限与抓取上限。
  - `7天 周报` 不再只是把日报时间窗口调成 7 天；周报会单独放宽热点覆盖数量，目标改为 `6-8` 条一周热点。
  - `agents/deep_analyst.py` 已支持按报告模式动态调整新闻数量目标、摘要长度与总结要求；周报会额外生成“本周主题总结”。
  - `tools/export_word.py` 与 `tools/export_ppt.py` 已支持周报标题切换，并在每个专题前单独输出“本周主题总结”。
  - Streamlit 预览与运行状态面板已能显示当前报告模式；周报模式下，深度新闻预览会先展示专题总结。
- 已把上述代码同步到本地运行副本，关键文件哈希一致。

## 3. 未完成内容

- 尚未跑一次带真实搜索/API key 的完整端到端烟测。
- 尚未用真实公司专题确认“第二轮放宽后的核心时间线”是否在数量和相关性上都达到预期。
- 尚未用真实 `Trump` 专题确认中东局势是否已稳定进入核心时间线。
- 尚未用真实 `Nvidia` 专题确认“长文反向补线”已彻底消除 1 条时间线对 3 条长文的断层。
- 尚未用真实 `Tesla` 专题确认中国 FSD 备案/审批/试点信号是否已稳定浮到前排。
- 尚未检查 Word 导出在长时间线场景下的观感。
- 尚未用真实 `7天 周报` 专题确认：
  - 一周热点覆盖数量是否稳定达到 `6-8` 条左右；
  - “本周主题总结”是否真正总结了周主线，而不是把日报口径简单拉长；
  - PPT / Word 周报导出页数与阅读节奏是否仍可接受。

## 4. 关键决定

- `AGENTS.md` 只写长期有效、低频更新的稳定规则，不写这次任务的临时细节。
- `PLANS.md` 只记录本轮任务的目标、边界、步骤和风险。
- `HANDOFF.md` 用来承接当前进度，后续每次有实质性实现或验证都应继续更新。
- 真实验证默认在本地运行副本执行；代码修改默认在 Git 同步仓库完成并再同步过去。
- 当前任务不做无关重构，优先改最短路径上的问题点。
- 核心时间线与深度新闻分层处理：
  - 时间线负责保留更完整的“当天值得记住的更新”。
  - 深度新闻继续只展开其中更值得详细分析的子集。
- 时间线优先保证“覆盖面”而不是“每条都能展开成长文”：
  - 对公司、宏观、政策和地缘专题，允许保留更多短讯型更新。
  - 不再把“是否适合写长文”当作时间线保留前提。
- 当前实现接受“深度新闻先识别出更多有效事件，再由报告层反向补线”：
  - 这是一种输出层修复，目标是先确保最终给用户看到的时间线与长文一致。
  - 如果后续需要更强的一致性，再继续前移到 `select_analysis_candidates()` 或 `timeline_agent` 本身。
- 当前实现优先采用“低 token 规则增强”而不是“继续扩大模型输入”：
  - 先用现成搜索结果补齐短讯，再用较小的启发式阈值去噪。
  - 只有当规则补位仍明显不足时，才考虑继续增加模型输入或 Prompt 复杂度。
- 当前模型链路默认固定为 DeepSeek：
  - Gemini 相关设置暂不纳入本轮验证与故障定位范围。
- PPT 核心时间线页只保留短讯主干和极短承接提示；详细匹配原因留在调试视图或深度新闻链路。
- 当前已明确区分“日报”和“周报”：
  - `24h 日报` 继续沿用原有较紧的 24 小时窗口与较少热点条数；
  - `7天 周报` 使用更宽的候选与抓取配额，并要求输出“本周主题总结”。
- 当前周报不是另起一套全新 agent，而是在现有链路上做参数分层和导出分层：
  - 优点是改动小、不会连带打坏日报；
  - 风险是如果后续还想继续做更重的周报专题归纳，仍可能需要单独的周报 prompt 或 query 设计。

## 5. 风险/禁区

- 当前同步仓库是脏工作树，不要回退或覆盖已有未提交改动。
- 不要修改或提交真实密钥文件，例如 `.streamlit/secrets.toml`。
- 不要默认改动 `.venv/`、`.idea/`、`__pycache__/`、生成产物和日志文件。
- `template.pptx` 属于高风险文件，除非确认问题根因在模板本身，否则不要先动它。
- 当前实现已经完成，但在真实联网跑通前，不要把“效果已验证”写成既成事实。
- 时间线数量放宽后，后续如果发现噪声回升，优先微调去重阈值、输入窗口和 topic pack，不要立刻再收紧回旧逻辑。
- 当前新增的“反向补线”会把深度新闻阶段识别到的重要事件带回时间线：
  - 这能修复时间线过少和前后脱锚；
  - 但如果深度新闻阶段本身混入低相关度事件，时间线也会被一起补进去，因此后续仍需关注长文阶段的选题质量。
- 当前新增的“规则补位”主要依赖标题和来源启发式：
  - 优点是几乎不增加 token；
  - 风险是对表述特别绕、标题特别弱或多语种混杂的新闻，仍可能补位不足或去重不够理想。
- `Trump` 这类政策/地缘专题与 Apple / Google / Tesla 这类科技公司专题不同：
  - 评估时要接受更高比例的政策、能源、外交和制裁类短讯。
  - 不要再用“硬件导向”标准去错误过滤这类专题的主线新闻。

## 6. 相关文件

- `agent_app.py`
- `tools/export_ppt.py`
- `tools/intelligence_packs.py`
- `tools/company_query_packs.py`
- `agents/timeline_agent.py`
- `agents/deep_analyst.py`
- `tools/report_linker.py`
- `.streamlit/secrets.toml.example`
- `AGENTS.md`
- `PLANS.md`
- `HANDOFF.md`

## 7. 验证方式

- 启动验证：在带 key 的本地运行副本中执行 `python -m streamlit run agent_app.py`，或使用副本中的 `run_local.ps1`。
- 功能验证：至少跑一次公司追踪流，确认新闻抓取、分析、金融补链和导出链路能走通。
- 版式验证：重点检查金融页 K 线图与文字区域是否重叠、是否压缩失真、是否越界。
- 时间线验证：抽查 Apple / Google / Tesla / Trump 等专题，确认核心时间线不再只剩会写成长文的少数事件，并且总量通常能达到 7-8 条左右。
- 对齐验证：抽查 `Nvidia` 这类专题，确认每条深度新闻都能在核心时间线里找到承接事件；若原始时间线漏掉，最终输出也应自动补线。
- 低 token 验证：确认时间线数量提升主要来自规则补位和候选去噪，而不是明显增加模型调用次数或提示词体积。
- 周报验证：在 `7天 周报` 模式下，确认周报标题、专题总结和热点数量都与日报模式明显不同，而不是只改了时间窗口。
- 主题验证：抽查苹果、谷歌、特斯拉及行业流结果，确认输出不再主要被泛 AI、隐私、诉讼类信息占据，而能看到硬件、供应链、量产和中国落地。
- Trump 专题验证：重点确认中东局势、关税、制裁、能源、白宫行政令等是否能同时进入时间线，而不是只剩单一经贸线。
- 结果记录：若外部接口失败或额度不足，需把失败点和未验证项明确记入交接说明。

补充说明：

- 已完成本地静态验证：`py_compile` 通过。
- 已完成本地结构烟测：使用本地 `.venv` 生成示例 PPT，确认金融页图表显示尺寸约为 `5.45" x 2.73"`，核心时间线页不再显示长段匹配原因。
- 已完成本地逻辑烟测：构造“1 条时间线 + 3 条长新闻”的 `Nvidia` 模拟案例后，`tools/report_linker.py` 会自动补成 3 条时间线，且 3 条长新闻都能显示承接时间线。
- 已完成本地逻辑烟测：构造“1 条时间线种子 + 4 条原始搜索结果”的 `Nvidia` 模拟案例后，`agents/timeline_agent.py` 会在不新增模型调用的情况下补成 3 条时间线，且重复事件明显减少。
- 已完成本地轻量烟测：`agents/deep_analyst.py` 的周报配置会稳定解析为 `6-8` 条热点，`tools/export_word.py` / `tools/export_ppt.py` 的封面标题可正确切换为“科技周报”。
- 本轮还需补做真实联网验证；当前文档中的“时间线更广”与“Trump 中东线补足”仍属于已实现、未完全实跑确认状态。

## 8. 下一步建议

1. 在本地运行副本用真实 key 跑一次 `Nvidia \\ Google \\ Tesla \\ Trump` 的公司追踪流，重点看：
   - Nvidia 是否不再出现“长文 3 条、时间线 1 条”的断层；
   - 时间线是否比之前更完整，且数量提升主要来自规则补位而不是新一轮模型扩容；
   - Google 是否明显出现 TPU / Pixel Fold / Tensor；
   - Tesla 是否明显出现 FSD 中国备案 / 审批 / 试点 / 功能开放；
   - Trump 是否明显出现中东局势 / 制裁 / 关税 / 白宫行政令。
2. 在同一批专题下，分别跑一次 `24h 日报` 与 `7天 周报`，重点比较：
   - 周报是否明显比日报覆盖更多热点，而不是只多出少量重复事件；
   - 周报是否稳定出现“本周主题总结”；
   - 周报专题数、单专题页数和总体导出体量是否仍可接受。
3. 导出真实 PPT / Word 后，重点查看：
   - 金融页 K 线图是否彻底脱离下方文本框；
   - 核心时间线是否仍然过于稀疏；
   - 周报新增的“本周主题总结”是否过短、过空或过日报化；
   - 周报一页一条热点新闻的体量是否还适合汇报阅读。
4. 如果真实时间线仍然偏少，优先继续调 `agents/timeline_agent.py`：
   - `TIMELINE_TARGET_MIN` / `TIMELINE_HARD_LIMIT`
   - 去重阈值
   - `_rewrite_event_dicts()` 的原始标题覆盖阈值
   - `policy` / `generic` / `business` 配额
   - 规则补位的支持度阈值、来源上限和标题压缩规则
5. 如果真实输出仍出现“长文先于时间线”的脱锚，优先继续排查：
   - `agent_app.py` 的 `select_analysis_candidates()`
   - `agents/deep_analyst.py` 的 event_id 回填
   - `tools/report_linker.py` 的匹配阈值与反向补线条件
6. 如果周报热点仍然不够全，优先继续调：
   - `agent_app.py` 中周报的 `company_search_per_query_limit` / `industry_search_max_results`
   - `agents/deep_analyst.py` 中周报的 `analysis_min_news` / `analysis_max_news`
   - 周报提示词里的“覆盖不同子主题”约束
7. 如果 Tesla 的中国 FSD 线仍然不够完整，优先继续补 `tools/company_query_packs.py` 的 Tesla pack 和中文站点覆盖，而不是先扩大模型输入。
8. 如果 Trump 线仍然不够完整，优先继续补 `tools/company_query_packs.py` 的 `trump` pack，而不是先收紧全局时间线。
9. 如果主题仍然不够硬件化，优先继续补 `tools/company_query_packs.py` 和 `tools/intelligence_packs.py`，不要先动搜索引擎基础设施。
